<%= text_area_tag "flow_node[content]", (node.type_collect? && node.content.present? ? node.content : {"prompt" => "", "attribute" => "", "validation" => "string"}).to_json, rows: 6, class: "input w-full font-mono", disabled: !(node.node_type == "collect") %>
<p class="text-xs text-gray-500">JSON: { "prompt": "...", "attribute": "email", "validation": "email|string|number" }</p>
