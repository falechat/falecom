<nav class="flex w-56 flex-col border-r border-slate-200 bg-white py-4 dark:border-slate-700 dark:bg-slate-900">
  <% @items.each do |item| %>
    <%= link_to item[:label], item[:href], class: "px-4 py-2 text-sm text-slate-700 hover:bg-slate-50 dark:text-slate-300 dark:hover:bg-slate-800" %>
  <% end %>
</nav>
